<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main bg-zinc-900 w-full min-h-screen p-10">

        <div class="form text-white">
            <form action="/create" method="post">
                <input class=" outline-none w-full bg-zinc-800 rounded-md my-1 px-5 py-2" type="text" name="title"
                    placeholder="Title" required>
                <textarea class="resize-none outline-none w-full bg-zinc-800 rounded-md my-1 px-5 py-2" name="note"
                    placeholder="Note goes here..."></textarea>
                <input class="bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Add Note">
            </form>
        </div>


        <div class="tasks flex flex-wrap gap-3 mt-8 justify-center">
            <%if(files.length> 0) { %>
                <% files.forEach(file=> { %>
                    <div class="task px-3 py-4 bg-zinc-800 rounded-md min-w-72">
                        <h2 class="text-3xl text-white tracking-tighter">
                            <%= file.split('.')[0] %>
                        </h2>
                        <div class="flex justify-between mt-3">
                            <a class="text-blue-500 inline-block text-sm" href="/file/<%=file%>">Read Note 📖</a>
                            <a class="text-blue-500 inline-block text-sm" href="/edit/<%=file%>">Rename ✏️</a>
                        </div>
                    </div>
                    <%})%>
                        <%} else { %>
                            <p class="text-zinc-500">Nothing here :(</p>
                            <% }%>
        </div>

    </div>
</body>

</html>